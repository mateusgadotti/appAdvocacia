@model IEnumerable<AppAdvocacia.Models.Agenda>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Clientes</h2>

@if (!Model.Any())
{
    <p>Não temos nada agendado ainda.</p>
}
else
{
    <p>
        @Html.ActionLink("Novo Agendamento", "New", null, new { @class = "btn btn-primary" })
    </p>
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Agenda</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var agenda in Model)
            {
                <tr>
                    <td>@Html.ActionLink(@agenda.Nome, "Edit", new { agenda.Id })</td>
                </tr>
            }
        </tbody>
    </table>
}